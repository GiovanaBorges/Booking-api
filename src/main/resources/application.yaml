spring:
    application:
        name: booking
        
    profiles:
        active: dev

    security:
        oauth2:
            resourceserver:
                jwt:
                    issuer-uri: http://auth-server:8080/realms/booking-realm